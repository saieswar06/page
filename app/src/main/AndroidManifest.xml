<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Page"
        android:usesCleartextTraffic="true"
        android:resizeableActivity="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        tools:targetApi="33">

        <!-- ✅ Launch screen -->
        <activity
            android:name=".SelectUserActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Anganwadi Worker Login -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="fullSensor" />

        <!-- ✅ Supervisor / Admin Login -->
        <activity
            android:name=".SupervisorLoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="fullSensor" />

        <!-- ✅ Worker Dashboard -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="fullSensor" />

        <!-- ✅ Admin Dashboard -->
        <activity
            android:name=".AdminDashboardActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="fullSensor" />

        <!-- ✅ Profile Page -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:screenOrientation="fullSensor" />

        <!-- ✅ Anganwadi Center Management -->
        <activity
            android:name=".admin.centers.AnganwadiCentersActivity"
            android:exported="false"
            android:screenOrientation="fullSensor" />

        <activity
            android:name=".admin.centers.AddCenterActivity"
            android:exported="false"
            android:screenOrientation="fullSensor" />

        <!-- ✅ ECCE Worker Management (optional)
        <activity android:name=".admin.users.ECCEUsersActivity" android:exported="false" />
        <activity android:name=".admin.users.AddECCEUserActivity" android:exported="false" />
        -->

    </application>
</manifest>
